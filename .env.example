# Copy to .env and fill in. Never commit .env.
# Generate values with e.g. `openssl rand -base64 24`

REDIS_PASSWORD=
POSTGRES_PASSWORD=

# Public base URL for webhooks. Set to your tunnel/proxy URL when
# Slack or another external service must reach n8n; defaults to localhost.
WEBHOOK_URL=
